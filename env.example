# =============================================================================
# ROOT – single source of truth for env (never commit .env)
# Copy to .env at project root. Backend and frontend reuse these values when
# run locally (they load root .env first). Docker Compose reads this file.
#
# Optional overrides per service: backend/.env, frontend/.env
# =============================================================================

NODE_ENV=development
FRONTEND_PORT=3002
BACKEND_PORT=4002
